<div class='container px-5 px-sm-0'>
    <div class='row'>
        <div class='col-md-3'>
            <%= render 'searches/search', range: Group %>
            <%=link_to new_group_path, class:'btn btn-outline-secondary btn-block mt-3' do %>
                <i class="fas fa-plus"></i>作成する
            <% end %>
        </div>
        <div class='col-md-7 offset-md-1'>
            <h2>カテゴリ</h2>
            <%= render 'index', groups: @groups %>
        </div>
    </div>
</div>

